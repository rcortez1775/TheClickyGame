import React from "react";
import "./Header.css";

const Header = props => (
  <header className="header">
    <h1>The Clicky Game!</h1>
    <h2>
      Click on any picture. But don't click it twice!
    </h2>
  </header>
);

export default Header;
